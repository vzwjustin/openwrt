#!/bin/sh

. /lib/functions/uci-defaults.sh

board_config_update

board=$(board_name)

case "$board" in
glinet,gl-be9300)
	ucidef_set_led_netdev "wan" "WAN" "white:wan" "wan"
	;;
esac

board_config_flush

exit 0
